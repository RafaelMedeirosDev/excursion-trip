# Frontend — Gestão de Excursões

SPA React consumindo a API NestJS de `apps/backend`. Este arquivo documenta o estado real da arquitetura do frontend — atualizar a cada feature, mesmo padrão do `apps/backend/CLAUDE.md`.

## Stack

Vite, React 19, TypeScript, Tailwind CSS (v3, formato clássico com `tailwind.config.ts` + CSS vars) + shadcn/ui, `react-router-dom`, `axios`, `@tanstack/react-query`, `zustand`, `class-variance-authority` + `clsx` + `tailwind-merge`, `lucide-react`, `react-hook-form` + `zod` + `@hookform/resolvers` (entraram na feature de `Events`, primeiro módulo com formulário de verdade).

## Estado atual

- **Bootstrap completo** (branch `feat/frontend-bootstrap`): Vite+React+TS rodando (`pnpm --filter @excursion-trip/frontend dev`, porta `3001` — mesma porta já configurada como `CORS_ORIGIN` no backend), Tailwind + base do shadcn/ui configurados, ESLint reaproveitando `packages/config/eslint.base.js` (mesmo padrão do backend) + plugins `react-hooks`/`react-refresh`.
- **Login funcional de ponta a ponta contra o backend real**: `LoginPage` → `POST /auth/login` → guarda `accessToken` (memória) + `refreshToken` (`localStorage`) → redireciona pro `Dashboard`, protegido por `PrivateRoute`. `Sair` chama `POST /auth/logout`. Testado via curl simulando as chamadas exatas do frontend (login, decode do JWT, refresh com rotação, preflight de CORS) — sem ferramenta de navegador disponível no ambiente de desenvolvimento assistido, então a renderização visual em si não foi conferida automaticamente, só a camada de rede/lógica.
- **Páginas existentes até agora**: `LoginPage` (`/login`), `DashboardPage` (`/`), `EventsPage`/`EventDetailsPage`/`CreateEventPage` (`/events`, `/events/:id`, `/events/new`), `ExcursionsPage`/`ExcursionDetailsPage`/`CreateExcursionPage` (`/excursions`, `/excursions/:id`, `/excursions/new`), `CustomersPage`/`CustomerDetailsPage`/`CreateCustomerPage` (`/passengers`, `/passengers/:id`, `/passengers/new`), `SuppliersPage`/`SupplierDetailsPage`/`CreateSupplierPage` (`/suppliers`, `/suppliers/:id`, `/suppliers/new`), `UsersPage`/`UserDetailsPage`/`CreateUserPage` (`/users`, `/users/:id`, `/users/new`), `VehicleBookingsPage`/`VehicleBookingDetailsPage`/`CreateVehicleBookingPage` (`/vehicles`, `/vehicles/:id`, `/vehicles/new`), `BoardingPointsPage`/`BoardingPointDetailsPage`/`CreateBoardingPointPage` (`/boarding-points`, `/boarding-points/:id`, `/boarding-points/new`), `ReservationsPage`/`ReservationDetailsPage`/`CreateReservationPage` (`/reservations`, `/reservations/:id`, `/reservations/new`), `PaymentsPage`/`PaymentDetailsPage`/`CreatePaymentPage` (`/payments`, `/payments/:id`, `/payments/new`), `NotFoundPage` (`*`). **Domínio operacional completo** — todos os 11 módulos do backend têm tela no frontend, com o ciclo de status de `Reservation` (`WAITLIST→PENDING→CONFIRMED`, além de `CANCELED`) todo ligado na UI.
- **Shell visual completo** (branch `feat/app-shell`): `AppLayout` agora compõe `Sidebar` + `Header` de verdade (antes era um placeholder de uma linha). `Sidebar`: navegação agrupada (`Operacional`/`Administrativo`), lida de `constants/navigation.ts` — único arquivo a tocar quando um módulo novo virar rota de verdade (`implemented: false → true`). Itens ainda não implementados aparecem esmaecidos com badge "em breve", sem link — evita cair num 404. Itens `adminOnly: true` (Eventos, Fornecedores, Veículos, Pontos de Embarque, Usuários) somem completamente da Sidebar pra `EMPLOYEE` (reforço de UX, não é a fonte de verdade de permissão — isso é o backend). Fixa no desktop, vira drawer com overlay em telas pequenas (`md:` breakpoint do Tailwind). `Header`: menu do usuário (`shadcn dropdown-menu`) com a `role` traduzida (`ADM`→"Administrador", `EMPLOYEE`→"Funcionário") e "Sair" — sem nome/e-mail, mesma limitação de JWT já documentada abaixo. `PageTitle` (`components/layout/PageTitle.tsx`) fica dentro do conteúdo de cada página, não no `Header` — cada página é dona do próprio título + ação opcional (ex.: botão "+ Novo" quando existir).

- **Módulo `Events` — primeiro módulo de domínio, vira o molde pros próximos** (branch `feat/events-module`): decisão do usuário de inverter a ordem original do plano (`Excursions` primeiro) — `Excursion.eventId` é obrigatório no backend, sem `Event` cadastrado não dá nem pra preencher o form de criar excursão. Backend só tem `create`/`list`/`get-by-id` pra `Event` (sem `update`), então **não existe `EditEventPage`** — mesma lacuna já documentada, não é esquecimento. Padrão por domínio replicável: `features/{domínio}/{api,types,validations,hooks,pages}` — `api/eventsApi.ts` (wrappers finos sobre `httpClient`), `validations/eventSchema.ts` (`zod`, espelha o DTO do backend campo a campo), `hooks/{useEvents,useEvent,useCreateEvent}.ts` (`TanStack Query`, `useCreateEvent` invalida a query de lista no sucesso), `pages/{EventsPage,EventDetailsPage,CreateEventPage}.tsx`. Form de criação usa `react-hook-form` + `zodResolver`, **sem componente `EventForm` separado** — só extrair quando existir um segundo consumidor de verdade (ex.: quando/se `Edit` existir), evita abstrair cedo demais.
  - **Correção posterior** (branch `fix/date-range-validation`, bug achado pelo usuário testando `Excursion`): `createEventSchema` ganhou `.refine()` rejeitando `endDate` antes de `startDate` (mesma correção em `createExcursionSchema` pra `returnDate`/`departureDate`) — espelha a validação nova do backend (`EventInvalidDateRange`/`ExcursionInvalidDateRange`, `400`), sem mudança de página (o erro usa o mesmo `errors.endDate`/`errors.returnDate` que já era renderizado pra "campo obrigatório").
  - **Campo `state` (UF), branch `feat/event-state-field`**: pedido do usuário testando o cadastro de Evento. **Primeiro uso real de `packages/shared`** — `UF`/`UF_LABELS` vêm de `@excursion-trip/shared` (`workspace:*` em `dependencies`, resolvido via `pnpm install` normal, sem alias novo no `tsconfig` — é pacote de workspace de verdade, não path relativo tipo `@/*`) em vez de redeclarado em `features/events/types`. `CreateEventPage` ganhou `Select` de UF via `Controller` (opções de `Object.entries(UF_LABELS)`), `EventsPage` mostra a sigla numa coluna nova, `EventDetailsPage` mostra o nome completo via `UF_LABELS[event.state]`. `createEventSchema` usa `z.enum([...27 siglas literais])` — não dá pra derivar 100% do tipo `UF` do `shared` dentro do `z.enum`, precisa repetir a lista (aceitável, é só uma constante).
  - **`EventsPage` ganha paginação de verdade** (branch `feat/events-pagination`, quinta paginação do projeto, mesmo padrão de `Excursions`/`Reservations`/`Payments`/`Customers`): novo hook `usePaginatedEvents` chama `GET /events/paginated`; `useEvents()` **não muda**, continua usado pelos outros 8 consumidores (é a entidade mais reaproveitada pra lookup do projeto — `Dashboard`, `VehicleBookingsPage`, `CreateVehicleBookingPage`, `ReservationsPage`, `CreateReservationPage`, `CreateExcursionPage`, `PaymentsPage`, `CreatePaymentPage`). Busca de 1 campo só (`name`, o próprio nome do evento). Mesmo debounce (~300ms) e reset de página durante o render já usados nas quatro paginações anteriores. Mesmos controles de paginação (`Button` "Anterior"/"Próxima" + "Página X de Y").
- **`AdminRoute`** (`routes/AdminRoute.tsx`, novo — mesmo padrão de `PrivateRoute`/`PublicRoute`): guarda de rota por role, `<Navigate to="/" />` se `!hasRole('ADM')`. Antes só a `Sidebar` escondia o link pra `EMPLOYEE` — a rota em si ficava acessível digitando a URL direto. `Events` é o primeiro módulo a usar, mas é genérico — todo módulo `adminOnly` (Vehicles, BoardingPoints, Users) deve envolver suas rotas com ele. Continua sendo reforço de UX, não fonte de verdade — o backend já bloqueia com `403` independente disso (confirmado testando: `EMPLOYEE` batendo direto na API sem passar pela UI recebe `403` do `RolesGuard`).
- **`EmptyState`** (`components/feedback/EmptyState.tsx`, novo, compartilhado) e **`shadcn table`/`skeleton`** entraram nessa feature — primeiro módulo com listagem de verdade (loading via skeleton, vazio via `EmptyState`, erro `404` de `GET /:id` tratado com `isAxiosError(error) && error.response?.status === 404`, padrão a repetir nos próximos módulos).

- **Módulo `Excursions`** (branch `feat/front-excursions-module`): segundo módulo de domínio, mesmo molde do `Events` acrescido de ciclo de vida de status. Backend confirmado 100% `ADM`-only nas 4 rotas (`POST/GET/GET :id/PATCH :id/status`) — `navigation.ts` corrigido pra `adminOnly: true` (o item já nasceu errado como `false` no scaffold do app-shell, antes do módulo existir de fato; confirmado com `EMPLOYEE` recebendo `403` direto na API). Mesma assimetria list/detail de sempre: `GET /excursions` devolve a entidade com a relation `event` embutida (`ExcursionWithEvent`), `GET /excursions/:id` devolve a entidade crua (`Excursion`, sem `event`) — por isso a `ExcursionDetailsPage` busca o evento relacionado separadamente. Sem `update` de campo geral, só a máquina de transição de status (`PATCH /:id/status`), mesma lacuna de sempre — sem `EditExcursionPage`.
  - **Exceção nova, registrada**: hooks de leitura de um domínio genuinamente relacionado podem ser reaproveitados por outro — `CreateExcursionPage` usa `useEvents()` (popular o `Select` de evento) e `ExcursionDetailsPage` usa `useEvent(excursion.eventId)` (mostrar nome/cidade do evento relacionado), ambos de `features/events/hooks`. Regra continua: **só hooks de leitura são compartilháveis — componentes de UI e mutações continuam exclusivos de cada domínio.**
  - **`ALLOWED_TRANSITIONS`** espelhado do backend em `features/excursions/constants.ts` só decide quais botões aparecem (`PLANNING→OPEN|CANCELED`, `OPEN→CLOSED|CANCELED`, `CLOSED→DONE|CANCELED`, `DONE`/`CANCELED` sem transição) — o backend segue validando de verdade, o frontend não é fonte de verdade da máquina de estados.
  - **`ConfirmDialog`** (`components/feedback/ConfirmDialog.tsx`, novo, compartilhado, primeiro uso): `AlertDialog` genérico pra confirmações simples (`{ trigger, title, description?, confirmLabel?, onConfirm }`), usado pelas transições `OPEN`/`CLOSED`/`DONE`. A transição pra `CANCELED` usa um dialog próprio do domínio (`CancelExcursionDialog.tsx`, não genérico) porque exige `cancelReason` obrigatório — usa `Button` simples com `open`/`setOpen` controlados manualmente em vez de `AlertDialogAction` (que fecha o dialog imediatamente ao clicar, antes da mutation assíncrona resolver ou mesmo se a validação falhar).
  - **`Select` do shadcn com `react-hook-form`**: como o `Select` não expõe um `ref`/`onChange` compatível com `register()`, o campo `eventId` do form de criação usa `Controller` (`react-hook-form`) em vez de `register`.
  - **`ExcursionsPage` ganha paginação de verdade** (branch `feat/excursions-pagination`, primeira paginação do projeto — confirmado via `AskUserQuestion` que devia ser no backend, não só um corte visual em cima da lista inteira): novo hook `usePaginatedExcursions` (ao lado de `useExcursions`, que **não muda** — continua usado por `DashboardPage`/`CreateVehicleBookingPage`, que precisam da lista completa) chama a rota nova `GET /excursions/paginated`, devolvendo `{ data, total, page, limit }`. `limit` fixo em `10`, `page` como estado local resetado pra `1` sempre que o filtro de status ou a busca por evento mudam (senão a tela ficaria "presa" numa página que deixou de existir). **Reset de página feito durante o render, não em `useEffect`** — `eslint-plugin-react-hooks` (regra `set-state-in-effect`) rejeita `setState` síncrono dentro do corpo de um efeito; o padrão usado é o mesmo documentado no guia oficial do React pra "ajustar estado quando uma prop muda": comparar o filtro atual contra uma cópia guardada em estado e chamar `setPage(1)` direto no corpo do componente quando ela diverge, não dentro de `useEffect`. A busca por evento (`Input`, antes filtrada 100% no navegador) agora dispara uma chamada de rede a cada mudança — ganhou **debounce local simples** (`useEffect`+`setTimeout`, ~300ms, sem lib nova, primeiro debounce do projeto). Controles de paginação são só 2 `Button` existentes ("Anterior"/"Próxima") + texto "Página X de Y" — **sem instalar o componente `pagination.tsx` do shadcn** (numeração com elipse seria mais peça do que uma ferramenta interna compacta pede), só aparecem quando `total > limit`. `placeholderData: keepPreviousData` (`@tanstack/react-query` v5) evita a tela piscar pro skeleton ao trocar de página.

- **Módulo `VehicleBooking`/`Veículos`** (branch `feat/front-vehicle-bookings-module`): sexto módulo de domínio, o motivo de `Suppliers`/`Users` terem sido feitos antes — `VehicleBooking` exige `supplierId`/`excursionId`/`userId`, as 3 dependências já existiam quando essa feature começou. Pasta `features/vehicleBookings/` (nome técnico da entidade), rota/label da Sidebar continuam `/vehicles`/"Veículos" (mesma convenção de `Customers`/`Passageiros`). Mesma assimetria list/detail de `Excursion`: `GET /vehicle-bookings` inclui as 3 relations (`excursion` cru, `supplier`/`user` sem campos sensíveis), `GET /vehicle-bookings/:id` devolve só a entidade crua (com os 3 IDs de FK) — a página de detalhe busca `excursion`/`supplier`/`user` separadamente. **Reaproveitamento de hooks de leitura entre domínios, agora com 4 hooks**: `useExcursions()`/`useSuppliers()`/`useUsers()`/`useEvents()` alimentam o formulário de criação (`useEvents()` só pra anexar o nome do evento na opção de cada excursão no `Select`, já que `excursion.eventId` sozinho não diz nada pro usuário), `useExcursion(id)`/`useSupplier(id)`/`useUser(id)`/`useEvent(id)` alimentam a página de detalhe (o evento vem de `useEvent(excursion.eventId)`, encadeado — só dispara depois que a excursão carrega). Na listagem, como o `excursion` embutido na resposta não traz o evento, a página busca todos os eventos de uma vez (`useEvents()`) e monta um `Map<eventId, name>` pra lookup por linha, em vez de 1 fetch por veículo (evita N+1 client-side).
  - **Duas regras de negócio do backend espelhadas na UX, sem duplicar a validação real**: o `Select` de excursão no formulário filtra client-side pra mostrar só `PLANNING`/`OPEN` (a API só aceita 1 status por vez em `?status=`, então busca tudo com `useExcursions()` sem filtro e filtra na mão) — criar veículo numa excursão `CLOSED`/`DONE`/`CANCELED` ainda dá `400` (`VehicleBookingExcursionNotAvailable`) se o filtro for contornado, tratado com mensagem específica. `plate` é único **por excursão**, não globalmente (`@@unique([excursionId, plate])`) — duplicar leva a `409` (`VehicleBookingAlreadyExists`), mesmo padrão de `cnpj`/`cpf`/`email` duplicado, só dispara quando `plate` é informado (é opcional).
  - **Primeiro formulário com campo monetário**: `value`/`price` são `Int` em centavos no backend (convenção do projeto). O formulário usa `Input type="number" step="0.01"` em reais e converte pra centavos (`Math.round(Number(valor) * 100)`) só no `onSubmit`, sem componente de máscara dedicado — decide-se se vale a pena extrair um helper compartilhado quando `Reservation.agreedValue`/`Payment.value`/`Expense.value` precisarem do mesmo tratamento.
  - **Pegadinha de tipos: `z.coerce.number()` não compõe bem com `register()` do `react-hook-form` quando o `useForm<T>` é tipado pelo tipo de saída do schema** — o `Resolver` gerado pelo `zodResolver` espera o tipo de entrada (`unknown` pros campos com `coerce`), gerando erro de build (`Type 'unknown' is not assignable to type 'number'`). Resolvido evitando `z.coerce` nos campos numéricos (`capacity`/`value`/`price`): ficam como `string` no schema (validados por regex, ex.: `/^\d+(\.\d{1,2})?$/`), convertidos pra `number` manualmente no `onSubmit` — mesmo lugar onde a conversão de centavos já acontece.
  - **`VehicleBookingsPage` ganha paginação de verdade** (branch `feat/vehicle-bookings-pagination`, oitava paginação do projeto, mesmo padrão de `Excursions`/`Reservations`/`Payments`/`Customers`/`Events`/`Suppliers`): novo hook `usePaginatedVehicleBookings` chama `GET /vehicle-bookings/paginated`; `useVehicleBookings()` **não muda**, continua usado pelos outros 8 consumidores. **`useEvents()`/`useReservations()` continuam exatamente como estavam** — o primeiro pro lookup de nome de evento na coluna, o segundo pro cálculo de "Vagas disponíveis" (`occupiedSeatsByVehicleId`, indexado por `vehicleBookingId`, então funciona igual em qualquer página exibida). Busca de 2 campos combinados (evento OU responsável), mesmo debounce (~300ms) e reset de página durante o render já usados nas paginações anteriores. Mesmos controles (`Button` "Anterior"/"Próxima" + "Página X de Y").

- **Módulo `BoardingPoint`/`Pontos de Embarque`** (branch `feat/front-boarding-points-module`): sétimo módulo de domínio, o mais simples desde `Customers` — sem status, sem checagem de duplicidade (nenhum campo `@unique`), sem gate de estado de excursão (diferente de `VehicleBooking`/`Reservation`, pode ser criado em qualquer status). `GET /boarding-points` inclui só 1 nível de relation (`vehicleBooking`, crua, sem `deletedAt`, **sem** a `excursion` dela aninhada) — como `useVehicleBookings()` (de `features/vehicleBookings/hooks`) já devolve `VehicleBookingWithRelations` com a `excursion` embutida, tanto o `Select` do form de criação quanto a listagem reaproveitam esse hook pra mostrar "tipo — placa (excursão)" sem precisar encadear `useExcursion`/`useEvent` como `VehicleBookingDetailsPage` faz. Na listagem monta-se um `Map<vehicleBookingId, label>` a partir desse hook (mesmo padrão do `Map<eventId, name>` de `VehicleBookingsPage`), evitando 1 fetch por linha. **Decisão de escopo pro detalhe, registrada de propósito**: `BoardingPointDetailsPage` só busca 1 nível de relação (`useVehicleBooking`, tipo/placa) — não encadeia até `Excursion`/`Event` como o detalhe de `VehicleBooking` — evita empilhar relações sem fim a cada módulo novo; se o usuário quiser esse nível de contexto aqui também, é ajuste pequeno a pedir depois (mesma lição do pedido que ampliou `VehicleBooking` com o evento).
  - **`BoardingPointsPage` ganha paginação de verdade** (branch `feat/boarding-points-pagination`, nona paginação do projeto, mesmo padrão das anteriores): novo hook `usePaginatedBoardingPoints` chama `GET /boarding-points/paginated`; `useBoardingPoints()` **não muda**, continua usado pelo `Select` de ponto de embarque em `CreateReservationPage`. **Primeira busca que a página ganha** (não existia filtro nenhum antes, nem quando o rollout de busca client-side passou pelas outras 8 listagens) — `Input` novo comparando só `address` (único campo textual da entidade), mesmo debounce (~300ms) e reset de página durante o render já usados nas paginações anteriores. `useVehicleBookings()` continua na página pro mesmo lookup de sempre (label "tipo — placa (excursão)"). Mesmos controles de paginação (`Button` "Anterior"/"Próxima" + "Página X de Y").

- **Módulo `Reservation`/`Reservas`** (branch `feat/front-reservations-module`): oitavo módulo de domínio, o mais complexo até agora — ciclo de status ligado a pagamento, escopo por linha e `Select` dependente pela primeira vez. **`ReservationStatus` entrou no `packages/shared` desde o início** (primeiro uso real, mesmo padrão do `UF` — não redeclarado local pra depois migrar); `STATUS_LABELS`/`STATUS_BADGE_CLASSES` ficaram locais em `features/reservations/constants.ts` (só usados dentro do próprio módulo, mesma situação de `Excursion`).
  - **Bloqueio de dependência real, resolvido na hora de planejar**: `/pending`/`/confirm` exigem soma de `Payment`s (≥50%/100% do `agreedValue`), mas `Payment.reservationId` é obrigatório — não dá pra ter `Payment` sem `Reservation` existir antes, então a ordem "Payments primeiro" que eu tinha sugerido estava errada (o usuário corrigiu). **Escopo dessa branch**: só `create`/`list`/`detalhe`/`cancelar` — `cancelar` não depende de pagamento. `ReservationStatusActions` só renderiza o botão "Cancelar" (quando `status !== "CANCELED"`); "Marcar como pendente"/"Confirmar" ficaram de fora até `Payments` existir de verdade, ligados depois (ver bullet abaixo).
  - **Sem `AdminRoute`**: nenhuma das 4 rotas tem `@Roles` — mesmo padrão de `Customers`, rotas ficam direto no `PrivateRoute`.
  - **Escopo por linha** (como `GET /reservations`/`GET /reservations/:id`/`POST /reservations/:id/cancel` do backend): `ADM` vê/mexe em tudo, `EMPLOYEE` só nas próprias — sem nenhuma lógica extra no frontend, o backend já filtra; confirmado testando os dois tokens.
  - **`Select` dependente, primeira vez no projeto**: o `Select` de ponto de embarque em `CreateReservationPage` usa `useWatch({ control, name: "vehicleBookingId" })` pra filtrar `useBoardingPoints()` (client-side, por `vehicleBookingId`) — só mostra pontos do veículo escolhido. Trocar de veículo reseta o campo (`setValue("boardingPointId", "none")` no `onValueChange` do `Select` de veículo) pra não submeter um ponto de embarque de outro veículo por engano. `Select` de veículo, por sua vez, reaproveita o mesmo filtro `PLANNING`/`OPEN` já usado em `VehicleBooking`.
  - **`Select` opcional sem valor vazio**: como o `Select` do shadcn não aceita `SelectItem value=""`, o "nenhum ponto de embarque" usa um sentinel (`"none"`) convertido pra `undefined` no `onSubmit` antes de montar o payload.
  - **4 relations na listagem** (`customer`, `vehicleBooking` — sem `excursion` aninhada, `boardingPoint` nullable, `user`), detalhe cru com 3 hooks cross-domain (`useCustomer`/`useVehicleBooking`/`useBoardingPoint`, só 1 nível cada, mesma decisão de escopo do detalhe de `BoardingPoint`). `agreedValue` segue o mesmo padrão de campo monetário de `VehicleBooking` (reais no formulário, convertido pra centavos no `onSubmit`).
  - Validado via curl com o cenário de dados mais elaborado até agora (organização + 2 usuários + evento + 2 excursões em status diferentes + fornecedor + 2 veículos + 2 pontos de embarque + cliente): create como `EMPLOYEE` com ponto de embarque válido, `409` de cliente+evento duplicado, `400` de excursão fechada, `404` de ponto de embarque de outro veículo, list com escopo por linha, detalhe cru, cancelar sem motivo (`400`)/com motivo (sucesso)/de novo numa já cancelada (`400`), `404` de id inexistente.
  - **Correção pedida depois do teste visual, mesma branch**: o usuário achou uma brecha — a checagem de duplicidade original era só por `vehicleBookingId`, deixando o mesmo cliente reservar ônibus/excursões diferentes do mesmo Evento. Backend passou a checar por Evento (`vehicleBooking → excursion → eventId`), excluindo reservas `CANCELED` do conflito (confirmado via `AskUserQuestion` — "ao mesmo tempo" significa só reservas ativas contam). Mensagem do `409` no frontend atualizada de "nesse veículo" pra "pra esse evento". **Evento acrescentado nas 3 telas**, mesmo pedido já feito em `VehicleBooking`: `ReservationsPage` ganhou coluna "Evento" (`Map` de 2 saltos — `useVehicleBookings()` pra achar o `eventId` a partir do `vehicleBookingId`, depois `useEvents()` pra achar o nome — já que a listagem de `Reservation` não traz a `excursion` aninhada dentro de `vehicleBooking`), `ReservationDetailsPage` ganhou card "Evento" (encadeando `useExcursion(vehicleBooking.excursionId)` → `useEvent(excursion.eventId)`, quarto/quinto hook cross-domain da página), `CreateReservationPage` passou a mostrar "excursão — evento" no `Select` de veículo (mesmo `useEvents()` lookup já usado em `VehicleBooking`/`BoardingPoint`).
  - **`ReservationsPage` ganhou filtro por Evento** (branch `feat/front-reservations-event-filter`, mesmo pedido já atendido em `PaymentsPage` — `feat/front-payments-vehicle-event-column`), combinável com o filtro de status que já existia. **100% client-side**, mesmo raciocínio de `Payment`: `GET /reservations` aceita `?status=` e `?vehicleBookingId=`, mas não `?eventId=` (um evento pode ter vários veículos/excursões), e como a lista já vem inteira do backend (só filtrada por status), filtrar por evento no front evita mexer no backend à toa. Precisou de mais um `Map` (`eventIdByVehicleBookingId`, ao lado do `eventNameByVehicleBookingId` que já existia pra coluna "Evento", mesma fonte `useVehicleBookings()`) e generalizou a mensagem do `EmptyState` (`hasActiveFilter = statusFilter !== "ALL" || eventFilter !== "ALL"`) pra cobrir os 2 filtros juntos, não só o de status.
  - **Os `Select` de evento (`Payments`/`Reservations`) viraram `Input` de texto, e mais 6 listagens ganharam busca** (branch `feat/front-search-filters`): o usuário apontou que um `Select` de evento não escala conforme a organização cadastra mais eventos — mesmo raciocínio já usado pra trocar `Table` por `Card` no mobile, aplicado agora à forma de filtrar. Confirmado via `AskUserQuestion`: em `Payments` o input **substitui** o `Select` de evento (não fica duplicado); em `Suppliers`, "número" citado pelo usuário é o telefone (não existe outro campo numérico no cadastro). **Padrão idêntico nas 8 páginas, sem componente de busca compartilhado** (mesmo princípio de não centralizar `CardField`/`formatCurrency` cedo): `useState("")` + `Input` do shadcn + `.filter()` com `.toLowerCase().includes()` sobre os dados já carregados, zero mudança de backend/hooks. `Excursions`/`Reservations` mantiveram o `Select` de status (`hasActiveFilter` passou a considerar os dois filtros juntos); as 6 sem filtro nenhum antes (`Customers`, `Events`, `Suppliers`, `VehicleBookings`, `Users` — `BoardingPoints` não foi pedida dessa vez) ganharam só o `Input`. Campos comparados por página: `Payments` — cliente ou evento; `Reservations`/`Excursions`/`Events` — nome do evento; `Customers` — nome ou CPF; `Suppliers` — nome, CNPJ ou telefone; `VehicleBookings` — evento ou nome do responsável; `Users` — nome, CPF ou e-mail. **`UsersPage` também ganhou a coluna/card de CPF**, que não existia na listagem (só no formulário de criação) — pedido explícito do usuário, já que o CPF virou um dos campos buscáveis.
  - **`ReservationsPage` ganha layout responsivo, piloto do projeto** (branch `feat/front-reservations-responsive-cards`): testando em tela estreita, o usuário achou ruim a `Table` do shadcn virar scroll horizontal (só ganha `overflow-x-auto`, sem reflow). Confirmado via `AskUserQuestion`: tabela continua no desktop (`hidden md:block`), mas abaixo do breakpoint `md` (mesmo breakpoint que `Sidebar.tsx` já usa pra alternar entre drawer mobile e sidebar fixa) vira uma lista de `Card` (`md:hidden`), 1 por reserva, mesmos dados e mesmo link pro detalhe — só a apresentação muda, a lista filtrada por trás (status + evento) é a mesma pros dois layouts. **Sem componente genérico novo** — cada página de listagem tem colunas/dados bem diferentes, um "data-table configurável" seria abstração prematura pra 1 único uso confirmado até aqui (mesmo princípio de `formatCurrency`/`Field` ficarem duplicados por página em vez de centralizados cedo); reaproveita só `Card`/`CardContent` do shadcn (já usado em todo `*DetailsPage.tsx`) e um `CardField` local (label pequeno/maiúsculo + valor, mesmo padrão do `Field` das páginas de detalhe). **Escopo explicitamente limitado a essa página por enquanto** — as outras 8 listagens (`Excursions`, `Payments`, `Customers`, `Events`, `Suppliers`, `VehicleBookings`, `BoardingPoints`, `Users`) usam a mesma `Table` e têm o mesmo problema, mas ficam pra depois de aprovar o resultado aqui; se o padrão se repetir, aí sim vale extrair um componente compartilhado.
  - **Rollout pras outras 8 listagens** (branch `feat/front-responsive-lists-rollout`, aprovado o piloto): mesmo padrão exato aplicado em `ExcursionsPage`, `PaymentsPage`, `CustomersPage`, `EventsPage`, `SuppliersPage`, `VehicleBookingsPage`, `BoardingPointsPage`, `UsersPage` — `hidden md:block` na tabela existente + `grid gap-3 md:hidden` com 1 `Card`/`CardField` (local, não centralizado, mesma decisão do piloto) por registro. Cabeçalho do card é nome/identificador + badge de status só onde já existia um badge (`ExcursionStatusBadge` em `Excursions`; as outras 7 entidades não têm conceito de status, cabeçalho fica só o nome). `VehicleBookingsPage` combina "tipo — placa" no cabeçalho do card (mesmo formato já usado no `vehicleLabel` de `BoardingPointsPage`/detalhe). Filtros já existentes (`Excursions`/`Payments`/`Reservations` por status ou evento) e o gate `hasRole("ADM")` no botão "Novo X" (`VehicleBookings`/`BoardingPoints`) não mudam — ficam fora da tabela/cards, no `PageTitle`/estado do componente. **Com isso, as 9 listagens do projeto são responsivas.**
  - **`ReservationsPage` ganha paginação de verdade** (branch `feat/reservations-pagination`, segunda paginação do projeto depois de Excursões, mesmo padrão): novo hook `usePaginatedReservations` chama `GET /reservations/paginated`; `useReservations()` **não muda**, continua usado pelos outros 5 consumidores que precisam da lista completa (`Dashboard`, `VehicleBookingsPage`, `CreatePaymentPage`, `PaymentsPage`, `VehicleBookingDetailsPage`). **A busca por evento (antes 100% client-side) passa a bater no backend** via `eventName`, com o mesmo debounce (~300ms) e o mesmo reset de página durante o render (não em `useEffect`) já usados em `ExcursionsPage`. **`useVehicleBookings()`/`useEvents()` continuam na página** — diferente de Excursion (que já embute o `Event` na resposta paginada), a resposta de `Reservation` só traz `vehicleBooking` cru, então o `Map` de 2 saltos pra exibir a coluna "Evento" continua necessário; só a filtragem mudou de lugar, não a exibição. Mesmos controles de paginação (`Button` "Anterior"/"Próxima" + "Página X de Y"), sem componente novo do shadcn.
- **`DashboardPage` fica mais rico** (branch `feat/front-dashboard-widgets`, pedido genérico do usuário — "trazer informações melhores" —, escopo fechado via `AskUserQuestion` com opções concretas antes de implementar): tudo calculado **no frontend**, reaproveitando hooks que já existiam (`useExcursions`, `useReservations`, `usePayments`, `useVehicleBookings`, `useEvents`), sem endpoint novo no backend.
  - **`ADM` ganha 4 cards de resumo** (grid `grid-cols-2 sm:grid-cols-4`): Excursões abertas (`useExcursions("OPEN")`, `.length`), Reservas por status (`useReservations()` sem filtro — `ADM` vê tudo —, contagem de `WAITLIST`/`PENDING`/`CONFIRMED`, `CANCELED` fica de fora de propósito, não foi pedido), Total arrecadado (`usePayments()`, soma `PAYMENT` − `REVERSAL`, mesmo cálculo já usado em `ReservationStatusActions`/`CreatePaymentPage`), Veículos ativos (`useVehicleBookings()`, conta `vehicleBooking.excursion.status` fora de `DONE`/`CANCELED`, campo já vem embutido em `VehicleBookingWithRelations`). **3 outras opções oferecidas não foram escolhidas** ("próximas excursões" com ocupação, bloco dedicado de "reservas por status", "pagamentos recentes") — ficam de fora, não implementadas especulativamente.
  - **"Meus Veículos" do `EMPLOYEE` ganha ocupação por veículo**: cada card passa a mostrar "X de Y confirmadas" (Y = `vehicleBooking.capacity`). Reaproveita `useReservations()` sem filtro — desde a feature de visibilidade, esse endpoint já devolve pro `EMPLOYEE` as reservas que ele registrou **ou** que pertencem a um veículo que ele é responsável, cobrindo "todas as reservas dos meus veículos" numa chamada só, sem precisar de 1 fetch por veículo (`?vehicleBookingId=` por linha seria N+1). Sem nenhum valor financeiro — só contagem, mantendo a mesma separação já usada (custo/faturamento continuam só pro `ADM`, ex.: campo "Custo" escondido em `VehicleBookingDetailsPage`).
- **`DashboardPage` do `ADM` ganha seção de Eventos** (branch `feat/front-dashboard-events-detail`, o usuário achou o Dashboard ainda vazio depois dos 4 KPIs e pediu especificamente eventos passados/futuros + detalhe por evento — avaliei a ideia e devolvi 2 perguntas concretas via `AskUserQuestion` antes de implementar, já que mudavam o resultado): card "Eventos" com 2 listas lado a lado (`grid md:grid-cols-2`) — "Próximos" (`event.endDate >= now`, ordenado por `startDate` crescente) e "Realizados" (`event.endDate < now`, ordenado por `endDate` decrescente). **Seleção por lista clicável foi a primeira versão**, mas o usuário testou e preferiu um `Select` — trocado ainda na mesma branch: um único `Select` com `SelectGroup`/`SelectLabel` ("Próximos"/"Realizados", primeiro uso de grupo dentro de `Select` no projeto — componente já existia no `select.tsx` do shadcn, só nunca tinha sido usado), `value={selectedEventId ?? undefined}`/`onValueChange={setSelectedEventId}` direto no estado (sem sentinel `"ALL"` — aqui não existe opção "todos", só "nenhum selecionado ainda", representado por `null`). **5º card de KPI, mesma branch**: "Eventos", com "Próximos" (`upcomingEvents.length`) primeiro e "Concluídos" (`pastEvents.length`) dentro do card (ordem pedida pelo usuário) — mesmos arrays que já alimentavam os grupos do `Select`, nenhum cálculo novo. **Card "Eventos" também virou o primeiro do grid** (era o último), a pedido do usuário. **Texto de debug removido**: "Login funcionando — organização {id}, role {role}" some, vira "Olá, {nome} — visão geral de {organização}." na `description` do `PageTitle`. **Duas tentativas**: a primeira foi buscar isso via `GET /organizations/me` novo (`features/organizations/` — `types`/`api`/`hooks`) — o usuário pediu pra reverter tudo isso e colocar `organizationName`/`name` direto no JWT em vez de um endpoint separado, já que o dado é estático por sessão (não muda sem novo login) e não justificava uma chamada HTTP a mais. `features/organizations/` foi **deletada por completo**; `lib/jwt.ts` (`JwtPayload`) ganhou `organizationName`/`name`, populados automaticamente pelo `useAuthStore` (que já decodifica o token inteiro em `user`, sem mudança nele) — `DashboardPage` passou a ler `user.organizationName`/`user.name` direto de `useAuth()`, sem hook novo nem fetch adicional — descrição final ficou só "Visão geral de {organização}." (a saudação com o nome foi tirada, pedido do usuário). `Header.tsx` também passou a mostrar `user.name` no menu (gatilho + label do dropdown) em vez de `ROLE_LABELS[user.role]` — `ROLE_LABELS`/`@excursion-trip/shared` saíram do arquivo, sem uso restante ali. Grid dos KPIs virou `grid-cols-2 sm:grid-cols-3 lg:grid-cols-5` (era `sm:grid-cols-4`) pra acomodar o 5º card. **Falso alarme no meio do caminho**: o usuário viu todos os KPIs zerados de uma vez (inclusive "Excursões abertas" como `—`, os outros como `0`) e achou que a mudança tinha "apagado" os dados — na real era sessão expirada (token de acesso de 15min + provavelmente o refresh token de 24h também vencido, sessão de teste rodando havia várias horas), confirmado meio a meio: `curl` no backend respondeu normal (`401` sem token, como esperado), e o padrão "tudo zerado ao mesmo tempo" é sintoma de request falhando, não de bug na lógica de agregação (que não muda entre os cards). Resolvido com logout/login de novo. Card "Detalhes do evento" só renderiza com `selectedEventId` setado, 4 números (mesmo visual dos KPIs gerais): Excursões (`Set` de `excursionId` a partir dos veículos do evento, sem hook novo — não precisou de `useExcursions()` sem filtro), Veículos, Vagas abertas (capacidade total menos ocupadas, formatado "X de Y"), Total arrecadado. **Regra de negócio confirmada via `AskUserQuestion`**: vaga ocupada = reserva `PENDING` **ou** `CONFIRMED`, não `WAITLIST` — decisão apoiada na própria semântica que o backend já documenta (`PENDING` = "garante a vaga" com 50% pago, `WAITLIST` = "lista de espera sem pagamento", ver `apps/backend/CLAUDE.md`). Todo o encadeamento evento→veículo→reserva→pagamento usa os mesmos 4 hooks que os KPIs já buscavam (`useEvents`, `useVehicleBookings`, `useReservations`, `usePayments`) — **zero chamada nova ao backend**, só um `Map` a mais (`eventIdByVehicleBookingId`, mesmo padrão já usado em `ReservationsPage`/`PaymentsPage`) pra cruzar reserva↔evento.
- **`ReservationStatusActions` ganhou "Marcar como pendente"/"Confirmar"** (branch `feat/front-reservation-status-actions`, feita depois do módulo `Payment` mergear — dependência que faltava desde `feat/front-reservations-module`): `useMarkReservationPending`/`useConfirmReservation` (novos hooks em `features/reservations/hooks`, mesmo padrão de `useCancelReservation` — mutation por `id`, sem payload, invalida `["reservations"]`/`["reservations", id]`) chamando `POST /reservations/:id/pending`/`/confirm`. Botões usam o `ConfirmDialog` compartilhado (`@/components/feedback/ConfirmDialog`), mesmo componente que `ExcursionStatusActions` já usa pra transição de status não-destrutiva — só pede confirmação, sem campo extra (diferente de `CancelReservationDialog`, que exige motivo). Regras de exibição espelham a máquina de estados do backend sem duplicar validação: "Marcar como pendente" só aparece em `WAITLIST`; "Confirmar" aparece em `WAITLIST` **ou** `PENDING` (backend permite pular etapa se pagar 100% de uma vez). Componente busca `usePayments()` (cross-domain, mesmo hook que `CreatePaymentPage` já usa) e mostra uma linha informativa "Já pago: R$ X de R$ Y" (soma `PAYMENT` − `REVERSAL` filtrando por `reservationId`, mesmo cálculo da caixa "já pago" do form de Pagamento) — só informativo, não bloqueia o clique, quem valida de verdade é o backend. Erro de cada mutation (`ReservationInsufficientPaymentForPending`/`Confirm`, `400`) mostrado como texto genérico em português abaixo dos botões, não o texto cru do backend (mesmo padrão de `CreatePaymentPage`/`CancelReservationDialog`). Validado via curl com o mesmo cenário de dados de `Reservation`/`Payment`: `pending` sem pagamento (`400`), pagar 50% + `pending` (sucesso, status vira `PENDING`), `confirm` com só 50% pago (`400`), completar 100% + `confirm` (sucesso, `CONFIRMED`), e uma segunda reserva confirmando direto de `WAITLIST` pra `CONFIRMED` (100% pago de uma vez, pulando `PENDING`).

- **Módulo `Payment`/`Pagamentos`** (branch `feat/front-payments-module`): nono e último módulo do domínio operacional — **só frontend**, `Payment` já estava 100% implementado e testado no backend, nenhuma mudança lá. `PaymentType`/`PaymentMethod` entraram no `packages/shared` desde o início (primeiro uso real, mesmo padrão de `UF`/`ReservationStatus`). Sem `AdminRoute` (nenhuma rota tem `@Roles`, mesmo padrão de `Customers`/`Reservations`), sem filtro de status na listagem (`Payment` não tem status — só `Excursion`/`Reservation` têm esse `Select`).
  - **Três decisões de profundidade de relation, cada uma no seu nível certo**: `PaymentsPage` (listagem) originalmente ficava em 1 salto — cliente via lookup em `useReservations()`, sem perseguir até `Event` (mesma lógica de não passar do necessário numa tabela já usada em `BoardingPointsPage`); **revisto depois** (ver bullet mais abaixo, branch `feat/front-payments-vehicle-event-column`) pra incluir Veículo/Evento também. `PaymentDetailsPage` vai fundo (mesmo padrão de `ReservationDetailsPage`): `payment.reservationId` → `useReservation` → cliente + `useVehicleBooking` → `useExcursion` → `useEvent`, cadeia de 5 hooks cross-domain, a mais longa do projeto até agora — página de detalhe não tem custo de N+1 como uma tabela teria.
  - **`Select` de reserva no formulário de criação filtra `status !== "CANCELED"`** — mesmo espírito do filtro `PLANNING`/`OPEN` de `VehicleBooking`: o backend não bloqueia pagamento numa reserva cancelada (sem gate nenhum em `CreatePaymentService`), o filtro é só UX pra evitar o caso óbvio.
  - **Caixa "já pago" no formulário de criação**: ao escolher uma reserva, soma os `Payment`s existentes dela (`PAYMENT` soma, `REVERSAL` subtrai — mesmo cálculo que `PendingReservationService`/`ConfirmReservationService` já fazem no backend) filtrando `usePayments()` (já carregado inteiro, sem endpoint novo — `GET /payments` não aceita filtro por `reservationId`, diferente de `GET /reservations` que aceita `?status=`) e mostra "já pago: R$ X de R$ Y" — só informativo, não bloqueia o envio, já que o backend também não valida limite superior.
  - Validado via curl (organização + 2 usuários + evento + excursão + fornecedor + veículo + cliente + reserva): create tipo `PAYMENT` como `EMPLOYEE`, create tipo `REVERSAL`, escopo por linha confirmado nos 2 tokens (`ADM` vê os 2, `EMPLOYEE` só o próprio), detalhe cru, `404` de pagamento inexistente, `404` de `reservationId` inexistente no create. `pnpm build`/`pnpm lint` limpos.
  - **`PaymentsPage` ganhou colunas "Veículo"/"Evento"** (branch `feat/front-payments-vehicle-event-column`, pedido do usuário depois de já estar usando a tela — achou que ajudava a entender de qual ônibus/evento é cada pagamento sem precisar abrir o detalhe). Sem mudança de backend, os dados já vinham nas respostas existentes: "Veículo" sai direto de `reservation.vehicleBooking` (já embutido em `ReservationWithRelations`, sem hook novo pra isso); "Evento" reaproveita o mesmo lookup de 2 saltos já usado em `ReservationsPage`/`CreatePaymentPage` (`useVehicleBookings()` hidratado com `excursion.eventId` + `useEvents()`, `Map<vehicleBookingId, eventName>`).
  - **Filtro por Evento, mesma branch, pedido em seguida**: `Select` acima da tabela (mesmo componente/sentinel `"ALL"` do filtro de status de `ReservationsPage`), opções a partir de `useEvents()` (já carregado pra coluna "Evento"). **100% client-side** — `GET /payments` não tem nenhum filtro no backend (diferente de `GET /reservations`/`GET /excursions`, que aceitam `?status=`), mas como a lista já vem inteira e sem paginação, filtrar no front evita mexer no backend à toa; mesmo espírito de outros filtros client-side já usados no projeto (`Select` de excursão em `VehicleBooking` filtrando `PLANNING`/`OPEN`). Precisou de mais um `Map` (`eventIdByVehicleBookingId`, mesma fonte do `eventNameByVehicleBookingId` que já existia pra coluna, só guardando o id cru em vez do nome) — `payment` → `reservation` (via `reservationById`) → `vehicleBookingId` → `eventId`, compara com o filtro selecionado.
  - **Correção pedida depois do teste visual, mesma branch**: o `Select` de reserva mostrava só "cliente (valor combinado)" — como um cliente pode ter várias reservas ativas (desde que pra eventos diferentes, regra fixada na feature anterior), ficava impossível diferenciar qual reserva era qual. Rótulo passou a ser "cliente — evento (valor combinado)", usando o mesmo lookup de 2 saltos já usado em `ReservationsPage`/`CreateReservationPage` (`useVehicleBookings()` pra achar o `eventId` a partir do `vehicleBookingId` da reserva, `useEvents()` pra achar o nome) — como a duplicidade agora é por evento, cliente+evento já identifica a reserva sem ambiguidade.
  - **`PaymentsPage` ganha paginação de verdade** (branch `feat/payments-pagination`, terceira paginação do projeto, mesmo padrão de `Excursions`/`Reservations`): novo hook `usePaginatedPayments` chama `GET /payments/paginated`; `usePayments()` **não muda**, continua usado pelos outros 3 consumidores que precisam da lista completa (`Dashboard`, `CreatePaymentPage`, `ReservationStatusActions`). **Só um parâmetro de busca** (`query`), não dois — a busca já era "cliente OU evento" combinados num `Input` só, então virou um parâmetro só no backend em vez de dois campos separados (diferente de `eventName` em `Excursions`/`Reservations`, que filtra 1 campo). Mesmo debounce (~300ms) e reset de página durante o render já usados nas duas paginações anteriores. `useReservations()`/`useVehicleBookings()`/`useEvents()` continuam na página pros mesmos lookups de sempre (Cliente/Veículo/Evento) — a resposta paginada de `Payment` só embute `reservation` cru, sem `customer`/`vehicleBooking` aninhados. Mesmos controles de paginação (`Button` "Anterior"/"Próxima" + "Página X de Y").

- **Funcionário passa a ver os veículos sob sua responsabilidade + reservas de qualquer veículo que puder ver** (branch `feat/employee-vehicle-visibility`, cross-cutting — mesmo precedente de `fix/date-range-validation`, backend e frontend juntos porque é uma regra de acesso só): 2 pedidos a partir de prints — nome do responsável faltando na lista de Veículos (dado já existia, só faltava exibir) e Dashboard vazio pro `EMPLOYEE`, sem jeito de ver os próprios veículos. Confirmado via `AskUserQuestion`: acesso **completo de leitura** (não só um resumo no Dashboard) — Sidebar, lista e detalhe de `Veículos`/`Pontos de Embarque` abertos pra `EMPLOYEE`, e "reservas do veículo" significa **todas**, de qualquer registrante, não só as que o próprio funcionário registrou.
  - **`VehicleBookingsPage`**: nova coluna "Responsável" (`vehicleBooking.user.name`, já vinha em `VehicleBookingWithRelations`, nenhum hook novo).
  - **Gate de criação por `hasRole`, primeiro caso do projeto de "lista aberta, criação restrita" na mesma página** (todo módulo anterior era ou tudo `AdminRoute` ou tudo aberto): `VehicleBookingsPage`/`BoardingPointsPage` escondem o botão "Novo X" com `useAuth().hasRole("ADM")` em vez de depender só da rota — `/vehicles/new`/`/boarding-points/new` continuam dentro do `<AdminRoute>`, só `/vehicles`, `/vehicles/:id`, `/boarding-points`, `/boarding-points/:id` saíram pro `<PrivateRoute>` direto. `navigation.ts`: os 2 itens viram `adminOnly: false` (o `Sidebar` já esconde grupo vazio sozinho, nada a mexer lá).
  - **`useReservations`/`reservationsApi.getReservations` ganham parâmetro `vehicleBookingId`** (mesmo padrão de `status`, query key `["reservations", { status, vehicleBookingId }]`) — usado por `VehicleBookingDetailsPage`, novo card "Reservas" (lista compacta: cliente, `ReservationStatusBadge`, valor, link pra `/reservations/:id`). **Hook ganhou terceiro parâmetro opcional `{ enabled }`** (`useQuery`'s `enabled: options?.enabled`, `undefined` mantém o comportamento de sempre pros outros call sites) — evita disparar a listagem sem filtro (todas as reservas do usuário) antes do `vehicleBooking.id` carregar, mesmo espírito do `enabled: Boolean(id)` já usado em hooks de recurso único como `useVehicleBooking`.
  - **`DashboardPage`**: seção "Meus Veículos" só quando `!hasRole("ADM")`, `useVehicleBookings()` já vem escopado pelo backend (sem filtro extra no front), mesmo lookup `Map<eventId, name>` via `useEvents()` já usado em `VehicleBookingsPage`. `ADM` não muda **nessa branch** (ver bullet abaixo — ganhou conteúdo próprio depois).
  - **Achado durante a investigação, não pedido diretamente**: `CreateReservationPage` já chamava `useBoardingPoints()` pra montar o `Select` de ponto de embarque, mas `GET /boarding-points` era `@Roles(Role.ADM)` — um `EMPLOYEE` criando reserva já batia num `403` silencioso nesse `Select` antes dessa branch (o `Select` ficava vazio, sem erro visível). Corrigido de quebra ao abrir a leitura de `BoardingPoint`.
  - Validado via curl com organização + 1 `ADM` + 2 `EMPLOYEE` + 2 veículos (1 de cada funcionário) + 1 reserva registrada pelo `ADM` no veículo do funcionário 1: `EMPLOYEE` lista só o próprio veículo, `404` no `GET /:id` do veículo do outro, `403` continua em `POST /vehicle-bookings`; `EMPLOYEE` não-dono vê `404` em `GET /reservations?vehicleBookingId=`, dono vê a reserva mesmo sem ter registrado ele mesmo (confirma "todos os registrantes"), `GET /reservations` sem filtro continua vazio pro dono (confirma que o escopo padrão não mudou); `GET /boarding-points` `200` pra qualquer `EMPLOYEE`. `pnpm --filter backend test` (137 testes), `pnpm build`/`pnpm lint` limpos.
  - **2 correções pedidas depois do teste visual do usuário com dados reais, mesma branch**: (1) `VehicleBookingDetailsPage` ficava com os cards "Excursão"/"Fornecedor"/"Responsável" travados em `skeleton` pra sempre pro `EMPLOYEE` — a página busca `Excursion`/`Supplier`/`User` por id via `useExcursion`/`useSupplier`/`useUser`/`useEvent`, e esses 4 `GET /:id` continuavam `@Roles(Role.ADM)` no backend, batendo `403` silencioso (sem mensagem de erro, só o skeleton nunca resolvendo). Corrigido no backend abrindo só o `GET /:id` dessas 4 entidades pra qualquer autenticado (`list`/`create` continuam como estavam). (2) O campo "Custo" (`vehicleBooking.value`, quanto a organização paga pelo aluguel do veículo) aparecia pro `EMPLOYEE` — informação administrativa/financeira que não devia ser visível pra quem só acessa o próprio veículo; escondido com `hasRole("ADM")` (mesmo hook de sempre), "Preço do assento" (`vehicleBooking.price`, o que o cliente paga) continua visível pros dois papéis. **Investigado e descartado como bug**: o usuário também reportou que o card "Reservas" não mostrava nada pro veículo que ele estava testando — conferido direto no banco (`prisma.reservation.findMany({ where: { vehicleBookingId } })`) que esse veículo específico realmente não tinha nenhuma reserva; os outros 3 veículos do mesmo funcionário tinham 1, 2 e 3 reservas cada, confirmando que o filtro funciona — o "bug" era só o veículo testado não ter dado ainda.

- **Módulo `Users`/`Usuários`** (branch `feat/front-users-module`): quinto módulo de domínio, criado antes de `VehicleBooking` (mudança de ordem combinada com o usuário — como `VehicleBooking.userId` também precisa de um `Select` de usuários, fez mais sentido ter o módulo completo em vez de um hook isolado). Estruturalmente igual a `Suppliers` (sem status, sem relations, `ADM`-only, usa `AdminRoute`), com duas diferenças: **sem assimetria list/detail** — `GET /users` e `GET /users/:id` devolvem o mesmo tipo (`Omit<User, 'password' | 'deletedAt'>`), o `password` nunca aparece em nenhuma resposta (confirmado testando: nenhum payload de rede contém o campo); e **`email` duplicado ou `cpf` duplicado geram o mesmo erro `409`** (`UserAlreadyExists`, sem diferenciar qual campo colidiu — `email` é único globalmente, `cpf` só por organização, mas o backend não distingue no erro), então o formulário mostra uma mensagem genérica de conflito em vez de apontar o campo específico (diferente de `Customer`/`Supplier`, que só tinham 1 campo único cada). `role` (`ADM`/`EMPLOYEE`) é o primeiro `Select` do frontend com opções fixas do enum, não uma lista vinda de outra API — usa `Controller` (mesmo motivo de sempre). **`EditUserPage` passou a existir** (ver bullet abaixo) — é a única entidade do projeto com tela de edição.
  - **Correção posterior** (branch `refactor/role-to-shared`): `Role`/`ROLE_LABELS` migraram pra `@excursion-trip/shared` — o tipo estava redeclarado em 4 lugares (`features/users/types`, `lib/jwt.ts`, `useAuth.ts`) e o rótulo em português duplicado de verdade em 2 (`features/users/constants.ts` e `Header.tsx`, mesmo conteúdo, arquivos diferentes — o risco de divergência que motivou a migração). `features/users/constants.ts` foi deletado (só continha `ROLE_LABELS`). `ExcursionStatus` **não** migrou junto — só é declarado uma vez (`features/excursions/types`), sem duplicação de verdade pra corrigir; fica de fora até que algum motivo concreto apareça.
  - **`EditUserPage` — primeiro formulário de edição do frontend inteiro** (branch `feat/user-edit`, backend e frontend na mesma branch, mesmo precedente cross-cutting de `fix/date-range-validation`/`feat/employee-vehicle-visibility`): rota `/users/:id/edit` dentro do `<AdminRoute>` que já existia (herda a proteção `ADM` sem código extra, nada a mexer em `navigation.ts` — não é item de menu), botão "Editar" no `UserDetailsPage` via a prop `action` do `PageTitle` (a prop já existia e nunca tinha sido usada nessa página).
    - **Botão "Editar" também na listagem** (ajuste pedido logo depois do teste visual: ter que abrir o usuário só pra descobrir onde fica a edição era um salto a mais sem motivo). Na tabela do desktop virou uma coluna "Ações"; a coluna "Nome" continua levando ao detalhe. **Botão só de ícone** (`Pencil`, `size="icon"` com `className="size-9"` pra manter a mesma altura do `size="sm"` que as outras listagens usam) — nasceu com o texto "Editar" ao lado, tirado a pedido do usuário logo em seguida; como ícone sozinho não tem nome acessível, ganhou `aria-label`/`title` "Editar usuário" (o `title` também dá o tooltip nativo no hover, já que o projeto não tem o `tooltip` do shadcn instalado). **Nos cards do mobile o card deixou de ser clicável por inteiro** — ele era um `<Link>` envolvendo tudo, e botão dentro disso seria `<a>` dentro de `<a>` (HTML inválido); o **nome** virou o link (igual à tabela) e o botão ficou ao lado, num `flex items-start justify-between`. Trade-off consciente: perde a área clicável cheia, ganha a ação visível, e as duas quebras de layout passam a se comportar igual. **Só `Users` tem esse botão** — é a única entidade do projeto com tela de edição. `usersApi.updateUser(id, payload)` (`httpClient.patch`) + `useUpdateUser` no molde exato de `useUpdateExcursionStatus` — mutation com `{ id, payload }` invalidando `["users"]` **e** `["users", id]`.
    - **Primeiro form do projeto que nasce preenchido**: todas as páginas anteriores são de criação, com o form vazio. Como o usuário só chega depois do primeiro render (`useUser(id)` é `useQuery`), a pré-carga é `reset(user)` dentro de um `useEffect` — não `defaultValues`, que só valeria no mount.
    - **Senha opcional na edição**: `updateUserSchema` estende `createUserSchema` deixando `password` como `z.union([z.string().min(6), z.literal("")]).optional()` — campo em branco significa "manter a senha atual" e é **removido do payload no `onSubmit`** (mandar `""` faria o `@MinLength(6)` do backend responder `400`, confirmado via curl).
    - **`Select` de perfil desabilitado quando o ADM edita a si mesmo** (`currentUser.sub === id`, via `useAuth`), com legenda explicando — o backend já bloqueia com `400` (`user_cannot_change_own_role`), a UI só evita oferecer a ação que sempre falha. Erros tratados por caso: `409` → "e-mail ou CPF já cadastrado"; `400` com `error === "user_cannot_change_own_role"` → mensagem específica; resto cai no texto genérico (mesmo padrão de `isAxiosError` do `CreateUserPage`).
    - **Sem componente `UserForm` compartilhado com `CreateUserPage`** — a duplicação é real, mas os dois formulários já divergem (senha obrigatória vs opcional, `Select` de perfil desabilitável, pré-carga, botão "Cancelar") e o projeto só extrai abstração com um segundo consumidor de verdade, mesma decisão registrada lá em `Events` ("sem componente `EventForm` separado").
  - **Exclusão (soft delete) com ícone na listagem** (branch `feat/user-delete`): `usersApi.deleteUser(id)` (`httpClient.delete`, rota `204`, sem retorno) + `useDeleteUser` no molde dos hooks de mutação só por id (`useCancelReservation`/`useMarkReservationPending`), invalidando `["users"]`. Segundo botão **só de ícone** ao lado do de editar nas duas quebras de layout (`Trash2`, `size="icon"` + `size-9` + `text-destructive` só no ícone — evita uma coluna de botões vermelhos sólidos), com `aria-label`/`title`, mesma solução de acessibilidade do ícone de editar.
    - Confirmação pelo **`ConfirmDialog` compartilhado** que já existia (`components/feedback/ConfirmDialog.tsx`, usado por `Excursions`/`Reservations`) — primeiro uso dele fora de transição de status. A descrição explica que o usuário perde acesso e sai da lista, mas o histórico é preservado.
    - **Desabilitado na própria linha** (`currentUser.sub === user.id`, via `useAuth`), com `title` explicando — mesma escolha do `Select` de perfil no formulário de edição: o backend bloqueia com `400`, a UI só não oferece a ação que sempre falha. Desabilitado em vez de escondido, pra não desalinhar a coluna. Nesse caso o botão é renderizado **fora** do `ConfirmDialog` (um `AlertDialogTrigger` com filho desabilitado não abriria de qualquer forma, e assim fica explícito).
    - **Guarda de paginação**: excluir o último item de uma página > 1 deixaria a lista vazia com os controles ativos — o componente volta uma página nesse caso, ajuste feito **durante o render** (não em `useEffect`), mesmo padrão que o reset de página do filtro de busca já usava por causa da regra `set-state-in-effect` do `eslint-plugin-react-hooks`.
    - Ícone **só na listagem**, como pedido — `UserDetailsPage` continua só com "Editar".
  - **`UsersPage` ganha paginação de verdade** (branch `feat/users-pagination`, décima paginação do projeto, mesmo padrão das anteriores): novo hook `usePaginatedUsers` chama `GET /users/paginated`; `useUsers()` **não muda**, continua usado pelo `Select` de responsável em `CreateVehicleBookingPage`. Busca por nome/CPF/e-mail (antes 100% client-side) passa a bater no backend via `query`, mesmo `Input` que já existia — só ganhou debounce (~300ms) e reset de página durante o render, mesmo padrão das paginações anteriores. Mesmos controles (`Button` "Anterior"/"Próxima" + "Página X de Y").

- **Módulo `Customers`/`Passageiros`** (branch `feat/front-customers-module`): terceiro módulo de domínio, o mais simples até agora — `Customer` não tem status, relations nem FK obrigatória de outro domínio. **Primeiro módulo onde o nome técnico da feature diverge do nome exibido**: pasta/arquivos seguem a entidade do backend (`features/customers/`, `CustomersPage`, `useCustomers`) mas a rota/label da Sidebar continuam como já estavam desde o `app-shell` (`Passageiros`, `/passengers`) — decisão explícita do usuário, não mudar o que já estava definido. **Primeiro módulo sem `AdminRoute`**: `POST/GET /customers` não têm `@Roles`, qualquer autenticado pode usar — as rotas `/passengers*` ficam direto dentro do `<PrivateRoute>`, fora do `<AdminRoute>` (confirmado testando `EMPLOYEE` criando/lendo passageiro sem `403`). Trata o `409` de `CustomerAlreadyExists` (cpf duplicado por organização) com mensagem específica na `CreateCustomerPage`, diferente do erro genérico usado em `Events`/`Excursions` (nenhuma das duas tinha checagem de duplicidade no create). `email` é opcional e nullable (`string | null` no tipo, `z.string().email().optional().or(z.literal(""))` na validação, convertido pra `undefined` no `onSubmit` antes de mandar pro backend) — exibido como "—" quando `null` na listagem/detalhe. Sem `EditCustomerPage` (sem `update` no backend, mesma lacuna de sempre).
  - **`EditCustomerPage` + ícone de editar na listagem** (branch `feat/customer-edit`, mesmo molde de `EditUserPage`: `useCustomer(id)` + `reset()` num `useEffect` pra pré-carregar, `409` tratado com mensagem específica de CPF, `usersApi`-equivalente `customersApi.updateCustomer` + `useUpdateCustomer` invalidando `["customers"]` e `["customers", id]`). Duas diferenças em relação a Usuários:
    - **Primeiro caso de "listagem aberta, edição restrita"**: a edição é `ADM`-only no backend, então a rota `/passengers/:id/edit` entra **dentro do bloco `<AdminRoute>`** enquanto as outras três rotas de `/passengers` continuam no `<PrivateRoute>`, e o ícone (listagem) e o botão "Editar" (detalhe) só renderizam com `hasRole("ADM")` — mesmo gate do `const canCreate = hasRole("ADM")` que `VehicleBookingsPage`/`BoardingPointsPage` já usavam pro botão "Novo X". Sem a rota dentro do `AdminRoute`, um `EMPLOYEE` chegaria na tela pela URL e só descobriria o bloqueio ao salvar (`403`).
    - **E-mail em branco limpa o cadastro**: o `onSubmit` manda `email: data.email ? data.email : null` — **`null`, não `undefined`**, que é o que o backend interpreta como "limpar" (`undefined` seria "não mexer"). O `CreateCustomerPage` continua mandando `undefined` no mesmo caso, porque na criação não há o que limpar. `updateCustomerSchema` reaproveita o `createCustomerSchema` inteiro (o `.or(z.literal(""))` do e-mail já servia).
    - Card do mobile deixou de ser clicável por inteiro, mesma troca já feita em `UsersPage` (`<a>` dentro de `<a>` é inválido) — aplicada mesmo pro `EMPLOYEE`, que não vê o botão, pra não manter duas estruturas de card diferentes na mesma página.
  - **`CustomersPage` ganha paginação de verdade** (branch `feat/customers-pagination`, quarta paginação do projeto, mesmo padrão de `Excursions`/`Reservations`/`Payments`): novo hook `usePaginatedCustomers` chama `GET /customers/paginated`; `useCustomers()` **não muda**, continua usado pelo `Select` de cliente em `CreateReservationPage`. **Só um parâmetro de busca** (`query`), mesmo caso de `Payments` — a busca já era "nome OU CPF" combinados num `Input` só. Mesmo debounce (~300ms) e reset de página durante o render já usados nas três paginações anteriores. Mesmos controles de paginação (`Button` "Anterior"/"Próxima" + "Página X de Y").

- **Módulo `Suppliers`/`Fornecedores`** (branch `feat/front-suppliers-module`): quarto módulo de domínio, criado como pré-requisito de `VehicleBooking` (que exige `supplierId`) a caminho de `Reservations` (que por sua vez exige `vehicleBookingId`) — mesma cadeia de dependência que já tinha feito `Events` vir antes de `Excursions`, só que um nível mais fundo. **Reverte uma decisão de arquitetura anterior**: até aqui `Supplier` estava documentado como "sem feature própria, só um `Select` embutido no form de `VehicleBooking`" — decisão do usuário de dar um módulo completo de verdade (lista/detalhe/criação + item novo `Fornecedores` na Sidebar, seção Administrativo), porque sem tela de cadastro o único jeito de criar fornecedor seria via Postman. Estruturalmente idêntico a `Customers` (sem status, sem relations, `cnpj` único por organização gerando `409` tratado com mensagem específica no create, mesmo padrão do `cpf`), mas **`ADM`-only** (usa `AdminRoute`, como `Events`/`Excursions` — diferente de `Customers`, que é aberto). `address` é o único campo opcional/nullable, exibido como "—" quando ausente. Sem `EditSupplierPage`. Primeira vez que um módulo nasce sem já ter um placeholder de rota na Sidebar — o item `Fornecedores` foi criado do zero em `navigation.ts`, não existia antes.
  - **`SuppliersPage` ganha paginação de verdade** (branch `feat/suppliers-pagination`, sétima paginação do projeto, mesmo padrão de `Excursions`/`Reservations`/`Payments`/`Customers`/`Events`): novo hook `usePaginatedSuppliers` chama `GET /suppliers/paginated`; `useSuppliers()` **não muda**, continua usado pelo `Select` de fornecedor em `CreateVehicleBookingPage`. Busca de 3 campos combinados (`query`, nome/CNPJ/telefone), mesmo caso de `Customers`/`Payments`. Mesmo debounce (~300ms) e reset de página durante o render já usados nas paginações anteriores. Mesmos controles (`Button` "Anterior"/"Próxima" + "Página X de Y").

## Arquitetura: por domínio (`features/`), não por tipo

Decisão registrada no plano de arquitetura inicial: `features/{domínio}/` (ex.: `features/auth/`, `features/excursions/`) concentra `api/`, `components/`, `hooks/`, `types/`, `pages/` daquele domínio. Camada compartilhada (`components/ui`, `components/layout`, `services/http`, `store`, `lib`) fica enxuta, só pro que é genérico de verdade — nada de regra de negócio de uma entidade específica aí. Motivo: o backend já tem 11 entidades, estrutura por tipo (`pages/`, `components/`, `services/` genéricos crescendo sem fim) não escala de forma legível nesse volume.

```
src/
  app/            # bootstrap: App.tsx, providers.tsx (QueryClient+Router), router.tsx (árvore de rotas)
  features/       # 1 pasta por domínio
    auth/
    dashboard/
    events/       # api, types, validations, hooks, pages — molde pros próximos módulos
  components/
    ui/           # shadcn/ui — Button, Input, Label, Card, DropdownMenu, Table, Skeleton (mais entram sob demanda via `pnpm dlx shadcn add`)
    layout/       # AppLayout (Sidebar+Header+Outlet), AuthLayout, Sidebar, Header, PageTitle
    feedback/     # EmptyState (mais entram conforme necessário — Loading, ConfirmDialog, ErrorBoundary)
  routes/         # PrivateRoute, PublicRoute, AdminRoute
  services/http/  # client.ts (instância axios) + interceptors.ts (token + refresh-and-retry)
  store/          # authStore.ts (zustand) — só estado de sessão, nunca estado de servidor
  lib/            # queryClient.ts, jwt.ts (decode), utils.ts (cn())
  config/         # env.ts
  constants/      # navigation.ts (itens da Sidebar — label/href/ícone/grupo/adminOnly/implemented)
  pages/          # páginas sem domínio (NotFoundPage)
  styles/         # globals.css (Tailwind + CSS vars do shadcn)
```

`Expense` e `Organization` **não** têm feature própria — decisão explícita do usuário: `Expense` como lista embutida nos detalhes de `Excursion`/`VehicleBooking`, `Organization` fora do fluxo operacional (criação é manual, fora do dia a dia). `Supplier` **tinha** essa mesma restrição, mas ganhou módulo completo na feature `feat/front-suppliers-module` (ver "Estado atual") — decisão revertida explicitamente pelo usuário.

## Autenticação

- **Tokens**: `accessToken` (JWT, 15min) só em memória (`authStore`, nunca em `localStorage`) — decodificado no client via `lib/jwt.ts` (`decodeJwt`, base64 puro, sem checar assinatura — validação de verdade é sempre no backend). `refreshToken` (opaco, 24h) em `localStorage`, sobrevive a F5.
- **Por que não cookie `httpOnly`**: o CORS do backend está com `credentials: false` hoje — não dá pra usar cookie sem mudar isso lá também. Documentado como decisão consciente de MVP, não definitivo.
- **Bootstrap de sessão** (`features/auth/hooks/useInitAuth.ts`): ao carregar a app, se existir `refreshToken` salvo, troca por um `accessToken` novo antes de renderizar rotas privadas (`App.tsx` mostra "Carregando..." enquanto isso resolve). Sem `refreshToken`, segue direto pro estado deslogado.
- **Interceptor** (`services/http/interceptors.ts`): injeta `Authorization: Bearer` em toda requisição; em `401` (fora das rotas `/auth/*`), dispara `POST /auth/refresh` **uma vez só por rajada** — requisições concorrentes reaproveitam a mesma Promise de refresh em vez de disparar N chamadas — e reexecuta a requisição original. Se o refresh falhar, limpa a sessão (`authStore.clear()`), o próximo `PrivateRoute` redireciona pro login.
- **`useAuth()`** (`features/auth/hooks/useAuth.ts`): `user` (payload decodificado do JWT — só tem `sub`/`organizationId`/`role`, **não tem nome nem e-mail**, ver limitação abaixo), `isAuthenticated`, `login()`, `logout()`, `hasRole()`.
- **Limitação conhecida do backend**: não existe endpoint `GET /auth/me` nem qualquer rota que devolva os dados do usuário logado além do que já está no JWT. `GET /users/:id` existe mas é `ADM`-only — um `EMPLOYEE` não consegue buscar os próprios dados por ali. Enquanto isso não for resolvido no backend, a `Profile` page e o nome no header ficam limitados ao que o token já carrega (`role`, `organizationId`, `sub`).

## shadcn/ui

- `components.json` configurado (`style: default`, `baseColor: slate`, `cssVariables: true`). Adicionar componente novo: `pnpm dlx shadcn@latest add {nome}` — copia o código pro repo (não é dependência de pacote), já sai formatado certo pro `cn()`/CSS vars daqui.
- **Pegadinha já resolvida**: o CLI do shadcn lê o `tsconfig.json` da raiz do app pra resolver o alias `@/*` — como esse projeto usa `tsconfig.json` só com `references` (padrão do Vite pra separar config de app/node), o CLI não achava o `paths` e criava uma pasta `@/` literal em vez de resolver pra `src/`. Corrigido duplicando `compilerOptions.paths` também no `tsconfig.json` raiz (além do `tsconfig.app.json`, que é o que realmente vale pra compilação). Se isso voltar a acontecer depois de mexer no tsconfig, é essa a causa.

## Convenções

Mesmo padrão de commit/branch do backend: 1 branch por feature/módulo, commits em português, sem trailer de co-autoria, usuário testa antes do push. Nomenclatura de arquivo: PascalCase pra componentes/páginas, camelCase pra hooks/services/utils. Ver o plano de arquitetura completo (rotas, bibliotecas avaliadas, padrão visual) na memória do projeto se precisar do racional completo por trás de cada escolha.
