generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models a definir junto com o usuario (ver secao 6 do plano de arquitetura).
